---
---

Correct session-forecast calibration so exported coverage measurements retain
their actual target semantics, cache keys include every shape-defining input,
and stored fingerprints stay aligned with the scoring corpus. Mismatched
coverage inputs now fail clearly instead of producing a mislabeled result.
