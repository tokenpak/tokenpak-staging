# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/
ENV/

# Node
sdk/node_modules/
node_modules/

# Testing & coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.log

# SQLite databases (local telemetry)
*.db
*.db-journal
*.db-shm
*.db-wal
telemetry.db*
monitor.db

# Environment / secrets
.env
.env.local
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS / OS
.DS_Store
*.icloud

# Docs build (mkdocs outputs to docs_site/ — see mkdocs.yml)
docs_site/
docs/_build/

# Local state
.tokenpak/
*.state.json
elo_ratings.json

# Build caches
.ruff_cache/
.mypy_cache/

# Temporary
.tmp/
*.tmp

# Internal audit and process files (never track in public repo)
*AUDIT*.md
*COVERAGE_GAPS*.md
*LAUNCH_CHECKLIST*.md
build/
proxy.py.bak*

# Backup / corrupted DB / typecheck reports — must never be tracked
*.bak
*.corrupt-*
monitor.db*
mypy*.txt

# Build / cache directories that should never enter the public tree
.benchmarks/
.grimp_cache/
.import_linter_cache/

# Workbench / agent state directories
.tokenpak/
.claude/

# Large generated artifacts (never track)
coverage.json
coverage_report.txt
benchmark.json
bandit-baseline.json
