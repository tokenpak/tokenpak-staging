# TokenPak pre-commit hook configuration
# Install: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files
# Docs: https://pre-commit.com

repos:

  # ── Python linting & formatting (Ruff) ────────────────────────────────────
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff
        name: ruff lint
        args: [--fix, --exit-non-zero-on-fix]
        types_or: [python, pyi]
      - id: ruff-format
        name: ruff format
        types_or: [python, pyi]

  # ── General file hygiene ────────────────────────────────────────────────
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
        name: Trim trailing whitespace
      - id: end-of-file-fixer
        name: Fix end of file (ensure newline)
      - id: check-yaml
        name: Validate YAML syntax
      - id: check-toml
        name: Validate TOML syntax
      - id: check-json
        name: Validate JSON syntax
      - id: check-merge-conflict
        name: Detect merge conflict markers
      - id: check-added-large-files
        name: Block large files (> 1MB)
        args: [--maxkb=1024]
      - id: debug-statements
        name: Check for debug statements (pdb, breakpoint)
      - id: check-case-conflict
        name: Check for case-insensitive filename conflicts

  # ── Secret detection ────────────────────────────────────────────────────
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.4.0
    hooks:
      - id: detect-secrets
        name: Detect secrets
        args: [--baseline, .secrets.baseline]
        exclude: tests/.*|\.env\.example

  # ── Public-surface guards (local hooks) ─────────────────────────────────
  # These keep workbench / internal language out of the public repo.
  - repo: local
    hooks:
      - id: forbidden-language
        name: No internal identity / workflow language in changed files
        language: pygrep
        # Patterns intentionally narrow: word-boundary on agent names so
        # product references (Claude Code, Anthropic, etc.) are unaffected.
        entry: '\bSue\b|\bCali\b|\bTrix\b|\bSuki\b|\bAya\b|\bDee\b|\bReiPo\b|\bopenclaw\b|\bfleet\b|TSR-[0-9]|TPS-[0-9]|CCI-[0-9]|MTC-[0-9]|OAS-[0-9]|TIP7-[0-9]|TRIX-MTC|WS-[0-9]|\.claude/projects|/home/sue/|trixxie168|\bStd 2[0-9]\b|\bStd 3[0-9]\b|Suki: auto-commit'
        types_or: [markdown, python, yaml, toml, shell, javascript, ts, json]
      - id: forbidden-files
        name: No backup / corrupt-DB / typecheck artifact files
        language: pygrep
        entry: '^(.*\.bak|.*\.corrupt-.*|monitor\.db.*|mypy.*\.txt)$'
        types: [file]

# ── Global exclusions ─────────────────────────────────────────────────────
exclude: |
  (?x)^(
    \.venv/|
    dist/|
    build/|
    .*\.egg-info/|
    htmlcov/|
    \.git/
  )$
