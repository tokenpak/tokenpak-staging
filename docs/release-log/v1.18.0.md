---
title: TokenPak v1.18.0 Release Log
version: 1.18.0
release_type: minor
status: in-progress
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-03T16:30:00Z
closed:
---

# TokenPak v1.18.0 Release Log

## Scope

First release under the consolidated release model: the public repository,
website, and documentation update at minor and major releases only, each
summarizing every change since the previous public version. This minor release
ships the recipe library, the public optimization-contracts surface, the
vector-database companion package, Pak wire markers with the canonical Pak
schema and `pak migrate`, and the vault-injection master switch (default OFF).
It removes the experimental `tokenpak.proxy.server_async` from the declared
public surface.

## Public API

Regenerated in a full-extras parity environment against the exact release tree:
**seven additions** (Pak marker constants, `pak create`/`pak migrate` command
surface, legacy-payload upgrade helpers) and **one removal**
(`tokenpak.proxy.server_async`), with the four known framework-example
sentinels unchanged. The removal is deliberate and documented in the changelog.

## Stage 1 — Trunk preparation

- Release-content base: the integration trunk at the bump merge (recorded at
  Stage 4 with the frozen SHA).
- Every version-bearing surface agrees on 1.18.0: `__version__`,
  `version.json`, documentation example payloads, and the API snapshot; the
  documentation-parity suite passes (86 checks).

## Stage 2 — Trunk validation

Recorded on the release candidate before tagging: the exact-head CI board on
the bump pull request, plus install/upgrade/rollback verification on the built
artifacts. Any omission is listed here explicitly before the tag gate.

## Stage 3 — Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all technical gates pass on the release commit? | At the tag gate | Exact-head CI, artifact integrity, and snapshot parity must be green on the final candidate. |
| 2 | Does trunk validation have zero failed checks? | At the tag gate | Stage 2 must complete with zero failures. |
| 3 | Are the release notes complete and reviewed? | At the tag gate | The summarized changelog and this record complete pre-tag notes. |
| 4 | Is rollback known to work for this release? | At the tag gate | Rollback to 1.17.1 must be executed and verified before tagging. |
| 5 | Are all breaking changes disclosed? | Yes | One deliberate surface removal (`server_async`), disclosed in the changelog and above; no other compatibility change is intended. |
| 6 | Is an active watcher assigned? | Yes | TokenPak owns the publication watch after verified publication. |

Decision: **GO only when every binding precondition is verified on the final
exact source and artifact pair. Until then, NO TAG.**

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.18.0"
```

No data migration is identified. Legacy Pak payloads upgrade explicitly via
`pak migrate`; legacy wire markers remain readable.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.17.1"
```

Published artifacts are immutable; a production problem is handled through
rollback, yank, or a superseding version, never by moving the released tag.

## Stage 4 — Production deploy

| Substep | Timestamp | Output / URL |
|---|---|---|
| Final public source freeze | | |
| Tag push | | |
| Build | | |
| GitHub Release | | |
| Package publication | | |
| Package-index verification | | |
| Clean-install verification | | |
| Documentation and website propagation | | |

## Stage 5 — Post-deploy validation

Pending production publication; runs from a fresh install from the production
package index.

## Stage 6 — Watch period

- **Watcher:** TokenPak
- **Watch start:** the verified publication timestamp
- **Watch length / end:** per release class, or as the release owner directs,
  recorded here either way

## Stage 7 — Closeout

- [ ] Release marked successful
- [ ] Shipped documentation reflects v1.18.0 behavior
- [ ] Changelog entry shipped as written
- [ ] Public release surfaces show v1.18.0
- [ ] Follow-up issues recorded
- [ ] Release communications recorded
- [ ] Watch period completed as recorded

Closed by: Pending

Closed at: Pending

## Exceptions / waivers

The test-index (TestPyPI) route remains unestablished host-wide. If it is still
absent at this release's pre-publish gate, the substitute local-artifact gate
applies (clean install, version and diagnostics checks, upgrade, and rollback
on the exact artifacts), recorded as a deviation exactly as in the previous two
releases.

## Retrospective

- **What dragged:** Pending closeout
- **What broke or nearly broke:** Pending closeout
- **Carry-forward action:** Pending closeout
