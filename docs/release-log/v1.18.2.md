---
title: TokenPak v1.18.2 Release Log
version: 1.18.2
release_type: patch
status: in-progress
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-05T07:20:33Z
closed:
---

# TokenPak v1.18.2 Release Log

## Scope

This compatible patch restores configured custom-provider loading and routing,
corrects compression/configuration claims to match the default HTTP proxy, and
adds conservative changed-surface CI. Validation also identified and corrected
prompt-path Companion persistence, monitor queue shutdown, and telemetry schema
initialization defects. The release includes directly affected tests,
documentation, version metadata, and generated release snapshots.

Out of scope: pricing, billing, public or operator-run storage migrations, TIP
wire changes, unrelated feature work, and replacement of existing required
staging contexts. Private local SQLite plumbing is in scope and upgrades
additively and automatically.

## Sign-offs

| Role | Name | Decision | Timestamp |
|---|---|---|---|
| Release owner | TokenPak | In progress | 2026-08-05T07:20:33Z |
| Independent review | TokenPak | Pending exact-commit acceptance | |
| Rollback decider | TokenPak | Roll back to 1.18.1 on failed validation or health | 2026-08-05T07:20:33Z |

## Stage 0 — Change readiness

| Area | Status | Evidence |
|---|---|---|
| Scope and compatibility | Pass | Backward-compatible provider repair, truthful compatibility settings, additive CI and doctor JSON fields, plus validation-driven private persistence and writer-lifecycle corrections. |
| Focused implementation checks | Pass | Classifier, provider, compression byte-fidelity, CLI, and configuration regressions passed before the version bump. |
| Full conservative validation | Pending | Exact candidate tests and artifacts are not complete. |
| Public identity and leak safety | Pending | Run against the immutable candidate before staging acceptance. |
| Documentation | In progress | Changelog, upgrade, rollback, limitations, and this release record are drafted. |

## Stage 1 — Integration preparation

- **Release candidate:** Pending canonical-identity commit
- **Staging pull request:** Pending
- **Build output:** Pending exact-candidate wheel and source distribution
- **Artifact hashes:** Pending

## Stage 2 — Full-conservative validation

| Check | Result | Evidence |
|---|---|---|
| Always-on trust baseline | Pending | |
| Risk classifier and selected-job result | Pending | |
| Existing required staging contexts | Pending | |
| Python, proxy, and documentation suites | Pending | |
| Node install, build, tests, and production audit | Pending | |
| Workflow and release-gate snapshots | Pending | |
| Wheel/source distribution build, metadata, contents, and purity | Pending | |
| Clean install and functional smoke | Pending | |
| Upgrade from 1.18.1 | Pending | |
| Rollback to 1.18.1 | Pending | |
| Public API and compatibility review | Pending | Generated symbol set must remain unchanged. |

## Stage 3 — Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all applicable technical gates pass on the exact release commit? | Pending | No staging merge before exact-head evidence. |
| 2 | Are all required and selected CI jobs successful and non-skipped? | Pending | Existing required contexts remain binding. |
| 3 | Is independent exact-head acceptance complete? | Pending | Separate acceptance is required. |
| 4 | Do release notes, limitations, upgrade, and rollback match the artifacts? | Pending | Recheck after build. |
| 5 | Is rollback to 1.18.1 verified? | Pending | Exercise with the exact artifacts. |
| 6 | Are breaking changes and migrations absent or disclosed? | Yes | No breaking change, public schema migration, or operator-run migration is required; private local stores upgrade additively in place. |
| 7 | Is the four-hour PATCH watcher ready? | Pending | Starts only after publication and fleet convergence. |

Decision: **NO-GO until every pending binding row is verified on the final exact head.**

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.18.2"
```

No operator-run data migration is required. Private local Companion and monitor
stores apply backward-compatible plumbing upgrades automatically.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.18.1"
```

Published artifacts are immutable. Deployment must retain a verified 1.18.1
environment and runtime-pointer backup until closeout.

## Compatibility disposition

- No breaking change is intended.
- No public data, configuration, registry, or storage-schema migration is
  required. Existing private local Companion and monitor databases apply
  idempotent additive columns and indexes automatically.
- The generated Python public-symbol set must remain unchanged.
- `tokenpak doctor --json` gains an additive `custom_providers` object with
  `configured`, `registered`, and `error` fields.
- Existing provider configuration continues to parse. Invalid provider entries
  remain non-fatal and are now distinguishable from successful registration.

## Known limitations

- `TOKENPAK_COMPACT`, `compression.enabled`, and the threshold are compatibility
  settings for the legacy explicit helper. They do not activate body compaction
  in the built-in default HTTP proxy path.
- Risk-selected CI is additive for v1.18.2. Existing required staging contexts
  remain authoritative until separate parity evidence supports migration.
- Custom providers must use a supported built-in wire format; invalid endpoint
  or format entries are skipped and reported rather than partially registered.

## Stage 4 — Publication

| Substep | Timestamp | Output / URL |
|---|---|---|
| Final public source freeze | | |
| Annotated tag push | | |
| Release workflow | | |
| Build and checksums | | |
| GitHub Release | | |
| Package publication | | |
| Neutral clean-install verification | | |

## Stage 5 — Public propagation and deployment

Pending verified publication. GitHub, documentation, website, registry, and
fleet surfaces will be dispositioned separately; a copied version string does
not prove propagation. Each deployment host retains rollback anchors and must
pass source, import, CLI, service, health, functional, and optional paid-service
checks before the version floor advances.

## Stage 6 — Watch period

- **Watcher:** TokenPak
- **Watch start:** Pending publication and fleet convergence
- **Watch length:** 4 hours
- **Signals:** package/release digest agreement, public-surface convergence,
  proxy health and restart count, request errors, custom-provider registration,
  byte-fidelity regression, deferred/dropped local writes, queue-drain health,
  and fleet version-floor health

## Stage 7 — Closeout

- [ ] Release marked successful
- [ ] Changelog and shipped documentation reflect v1.18.2 behavior
- [ ] Public source, tag, Release, PyPI, assets, and checksums agree
- [ ] Applicable documentation, website, and registry surfaces are dispositioned
- [ ] Every canonical host reports the 1.18.2 floor and passes health checks
- [ ] Four-hour PATCH watch completed
- [ ] Independent closeout acceptance recorded
- [ ] Follow-up work and retained limitations recorded

Closed by: Pending

Closed at: Pending

## Exceptions / waivers

None. An unavailable required release channel, test, acceptance, publication,
propagation, deployment, or watch signal is a hold until a governed disposition
is recorded here.

## Attachments / evidence

Pending exact candidate SHA, staging/public integration SHAs, CI URLs, artifact
hashes, tag object and peel, publication provenance, deployment receipts, watch
samples, and independent acceptance.

## Retrospective

- **What dragged:** Pending closeout
- **What broke or nearly broke:** Pending closeout
- **Carry-forward action:** Pending closeout
