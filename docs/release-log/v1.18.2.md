---
title: TokenPak v1.18.2 Release Log
version: 1.18.2
release_type: patch
status: complete
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-05T07:20:33Z
closed: 2026-08-07T07:48:13Z
---

# TokenPak v1.18.2 Release Log

## Scope

This compatible patch restores configured custom-provider loading, endpoint
routing, and outbound credential delivery; corrects compression/configuration
claims to match the default HTTP proxy; and adds conservative changed-surface
CI while pinning every third-party workflow action to a full peeled commit SHA
and requiring checksum verification before direct GitHub release downloads are
used, while repairing release-rehearsal dependency declarations. Validation
also identified and corrected prompt-path Companion persistence, monitor queue
shutdown, and telemetry schema initialization defects. The release includes
directly affected tests, documentation, version metadata, and generated release
snapshots.

Out of scope: pricing, billing, public or operator-run storage migrations, TIP
wire changes, unrelated feature work, and replacement of existing required
staging contexts. Private local SQLite plumbing is in scope and upgrades
additively and automatically.

## Sign-offs

| Role | Name | Decision | Timestamp |
|---|---|---|---|
| Release owner | TokenPak | Complete | 2026-08-07T07:48:13Z |
| Independent review | TokenPak | Accepted on the exact head (two adversarial rounds; second round verified all seven required contexts green on `a2056d690a80…` via live branch-protection query). Independent closeout acceptance recorded at closure. | 2026-08-07 |
| Rollback decider | TokenPak | Rollback path retained and unexercised: 1.18.1 environments and runtime-pointer backups preserved on every deployment host | 2026-08-07T07:48:13Z |

## Stage 0 — Change readiness

| Area | Status | Evidence |
|---|---|---|
| Scope and compatibility | Pass | Backward-compatible provider repair, truthful compatibility settings, additive CI and doctor JSON fields, repository-wide immutable workflow-action pins and digest-verified release fetches, plus validation-driven private persistence and writer-lifecycle corrections. |
| Focused implementation checks | Pass | Classifier, provider selection, real mock-upstream destination/path/query/body/credential delivery, credential non-overwrite/non-log, compression byte-fidelity, CLI, configuration, workflow topology, full-SHA action-pin, external-fetch checksum, and actionlint regressions passed before candidate freeze. |
| Full conservative validation | Pass | Governed CI at the exact head after the 2026-08-06 GitHub Actions infrastructure outage cleared: all seven required contexts green on `a2056d690a80…` and on the integration merge `88defbf7dfb4…`; tag-push workflow run 31138043262 green end to end. Advisory-tier exceptions are dispositioned in Stage 2. |
| Public identity and leak safety | Pass | Identity/language delta check green on the integration pull request; the promotion diff was verified to add zero restricted vocabulary. One structural `private-path` advisory matched the product's own documented vault-index default in `tokenpak/core/config_loader.py`; triaged as a false positive (line byte-identical to the base commit; the separate personal-home-path pattern did not match). A pattern refinement follows separately. |
| Documentation | Pass | Changelog, upgrade, rollback, limitations, release notes, and this release record are complete. |

## Stage 1 — Integration preparation

- **Release candidate:** `df9f34d4a5f0a231acb701c10b307fdf852fdd27` (canonical `TokenPak <hello@tokenpak.ai>` author+committer throughout)
- **Staging pull request:** `tokenpak/tokenpak-staging#697`, merged 2026-08-07T01:22:03Z at integration commit `88defbf7dfb492fccfcffca48c35d07d85de7461` (parents `f53b0f8eed…` + `a2056d690a80…`)
- **Build output:** Locally rehearsed at the exact candidate head (not the shipped artifact -- governed `release.yml` rebuilds at tag push)
- **Artifact hashes (local rehearsal only):** wheel `95f2016092326a6b0603b384953131d73bd438592a37e7864bc81865c3adde25`, sdist `2304a2991422704a6dc619cac2568d5c497bb8a1954e0abf6f9fb710b72d7610`

## Stage 2 — Full-conservative validation

| Check | Result | Evidence |
|---|---|---|
| Always-on trust baseline | Red — governed disposition | The advisory job concluded failure at the accepted head and at the integration merge on its one structural `private-path` finding: the product's own documented vault-index default (see Stage 0), triaged as a false positive (line byte-identical to the base commit; the personal-home-path pattern did not match). Not a required status context; recorded red with disposition rather than re-labeled green. The runs re-run to green after the 2026-08-06 GitHub Actions infrastructure outage were the identity/language, gate-inventory, and public-layout workflows. A pattern refinement follows separately. |
| Risk classifier and selected-job result | Aggregate red by rollup — governed disposition | The always-evaluated aggregate concluded failure by rolling up the advisory trust-baseline finding above; independent review confirmed no other cause was recorded against the classification or selected jobs. Not a required status context this release (risk-selected CI is additive — see Known limitations); recorded red with disposition rather than re-labeled green. |
| Existing required staging contexts | Pass | All seven required contexts green on `a2056d690a80…`, re-verified via live branch-protection query during second-round acceptance and unchanged at merge time. |
| Python, proxy, and documentation suites | Pass | tests/proxy/ 786 passed; tests/companion/+telemetry/ 934 passed; tokenpak/tests/test_custom_providers.py+test_proxy_config.py 58 passed; JS SDK 56 passed -- 0 unexpected failures across all runs at the exact head. Two independent review agents additionally ran these and adjacent suites themselves as part of adversarial verification (routing fix: reproduced the traversal exploit end-to-end + 36-case bypass battery, 0 bypasses; persistence fix: differential pre/post-fix testing on all 4 findings). The governed CI run subsequently confirmed at the exact head. |
| Node install, build, tests, and production audit | Pass | `packages/tokenpak-js` at exact head: `npm run build` clean (tsc + ESM build script, the new addition this release adds); `npm test` -> 6 suites / 56 tests passed; `npm audit --production` -> 0 vulnerabilities. |
| Workflow and release-gate snapshots | Pass | Release-gate snapshot validation green at the exact head and again in tag-push run 31138043262. |
| Wheel/source distribution build, metadata, contents, and purity | Pass | Local rehearsal at exact head `df9f34d4a5…`: `twine check` PASSED both artifacts; zero Pro/`packages/` content in the wheel; zero `_internal/` paths in the sdist. The binding governed build ran in tag-push run 31138043262 and produced the shipped artifacts recorded in Stage 4. |
| Clean install and functional smoke | Pass | Fresh venv install of the local wheel from a neutral cwd: `tokenpak --version` -> `tokenpak 1.18.2`; `tokenpak doctor` -> exit 0, 0 errors, 3 expected pre-setup warnings. Repeated post-publication against the shipped PyPI package (Stage 4). |
| Upgrade from 1.18.1 | Pass | Fresh venv: installed published `tokenpak==1.18.1` from PyPI, upgraded in place to the local 1.18.2 wheel. `--version` transitioned correctly; `doctor` exit 0 post-upgrade. |
| Rollback to 1.18.1 | Pass | Same venv, reverse direction: `pip install --upgrade tokenpak==1.18.1` over the local 1.18.2 install. `--version` -> `tokenpak 1.18.1`; `doctor` exit 0 post-rollback. |
| Public API and compatibility review | Pass | `scripts/release_gate/gen_api_snapshot.py --check` at the exact final head: "public-api snapshot matches on-disk". Symbol set unchanged. |

## Stage 3 — Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all applicable technical gates pass on the exact release commit? | Yes | All required contexts green on the accepted head `a2056d690a80…` and the integration merge `88defbf7dfb4…`; tag-push workflow run 31138043262 green. |
| 2 | Are all required and selected CI jobs successful and non-skipped? | Yes for required; advisory red dispositioned | All seven required contexts successful and non-skipped, verified via live branch-protection query during acceptance. The additive non-required trust-baseline job and its aggregate concluded failure on the triaged false positive; disposition recorded in Stage 2. |
| 3 | Is independent exact-head acceptance complete? | Yes | Two independent adversarial review rounds on the exact head; round two independently re-verified round one's remediations and the full context set. |
| 4 | Do release notes, limitations, upgrade, and rollback match the artifacts? | Yes | Release notes, changelog, limitations, and upgrade/rollback commands verified against the shipped wheel and sdist. |
| 5 | Is rollback to 1.18.1 verified? | Yes | Exercised in both directions during Stage 2 rehearsal; 1.18.1 environments and runtime pointers retained on every host through closeout. |
| 6 | Are breaking changes and migrations absent or disclosed? | Yes | No breaking change, public schema migration, or operator-run migration is required; private local stores upgrade additively in place. |
| 7 | Is the four-hour PATCH watcher ready? | Yes | Armed at publication; execution and early-close disposition recorded in Stage 6 and Exceptions. |

Decision: **GO** — every previously pending row verified on the final exact
head before publication actions began; publication executed 2026-08-07 as
recorded in Stage 4.

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.18.2"
```

No operator-run data migration is required. Private local Companion, monitor,
and telemetry stores apply backward-compatible plumbing upgrades automatically.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.18.1"
```

Published artifacts are immutable. Deployment must retain a verified 1.18.1
environment and runtime-pointer backup until closeout.

## Compatibility disposition

- No breaking change is intended.
- No public data, configuration, registry, or storage-schema migration is
  required. Existing private local Companion, monitor, and telemetry databases
  apply idempotent additive columns and indexes automatically, preserving
  legacy cost rows while filling missing columns in place.
- The generated Python public-symbol set must remain unchanged.
- `tokenpak doctor --json` gains an additive `custom_providers` object with
  `configured`, `registered`, and `error` fields.
- Existing provider configuration continues to parse. Invalid provider entries
  remain non-fatal and are now distinguishable from successful registration.
  Routable custom providers may omit `models`; `api_key_env` names the optional
  outbound credential source. Any non-empty client upstream credential wins.

## Known limitations

- `TOKENPAK_COMPACT`, `compression.enabled`, and the threshold are compatibility
  settings for the legacy explicit helper. They do not activate body compaction
  in the built-in default HTTP proxy path.
- Risk-selected CI is additive for v1.18.2. Existing required staging contexts
  remain authoritative until separate parity evidence supports migration.
- Custom providers must use a supported built-in wire format. Endpoints with
  userinfo, fragments, or credential-shaped query parameters and invalid format
  entries are skipped and reported rather than partially registered. A
  configured endpoint query is treated as fixed routing policy and wins a
  same-name request query field.

## Stage 4 — Publication

| Substep | Timestamp | Output / URL |
|---|---|---|
| Final public source freeze | 2026-08-07T01:22–01:26Z | Public `main` fast-forwarded to the exact accepted integration head `88defbf7dfb492fccfcffca48c35d07d85de7461`; no re-expression commit created; public `main` remains an ancestor of the integration trunk. |
| Annotated tag push | 2026-08-07T01:26:44Z | Annotated tag `v1.18.2` by `TokenPak <hello@tokenpak.ai>`, peels to `88defbf7dfb4…`. |
| Release workflow | 2026-08-07T01:26:53Z | Tag-push run 31138043262 (`Release TokenPak`), all jobs green: https://github.com/tokenpak/tokenpak/actions/runs/31138043262 |
| Build and checksums | run 31138043262 | Shipped wheel sha256 `b08d73aa8ae19f5f0d0bd856913fc336884416f796d7b9652a450aa06570ca18`; shipped sdist sha256 `471fa32c0d20f1ec9e6fc503bf454747d41df024ce37b1b7524d6b4d41227d2a`; `SHA256SUMS` published as a release asset. |
| GitHub Release | 2026-08-07T01:41:01Z | https://github.com/tokenpak/tokenpak/releases/tag/v1.18.2 (wheel, sdist, SHA256SUMS). |
| Package publication | 2026-08-07T02:38:25–27Z | PyPI `tokenpak 1.18.2`. The publish job was held by the package-index deployment environment's manual-approval protection rule and explicitly approved by the release owner before upload. |
| Neutral clean-install verification | 2026-08-07 (post-publish) | Fresh venv outside any source checkout: `tokenpak --version` -> `tokenpak 1.18.2` from the published package. An initial attempt from inside a source checkout read a stale version via `sys.path` cwd shadowing; discarded and rerun from a neutral cwd (procedural note retained in the retrospective). |

Note: changelog entry dates record the trunk cut date (2026-08-05 for this
release); publication timestamps are recorded in this table and on the release
object.

## Stage 5 — Public propagation and deployment

| Surface | Disposition | Evidence |
|---|---|---|
| Public source repository | Converged | `main` == tag `v1.18.2` == `88defbf7dfb4…`; source, tag, Release, and package index agree. |
| Documentation | Converged | Documentation revision `8324009946dad49e336335731fe73915b0ef40d4` records 1.18.2 as the current published release; the version-metadata documentation pin advances to that revision on the trunk in this completion change. |
| Website | No action required | Verified to carry no version-pinned strings affected by this patch. |
| Registry | No action required | Verified to carry no version-pinned strings affected by this patch. |
| Runtime — first deployment host | Converged 2026-08-07T02:45:39Z | Versioned 1.18.2 environment built from the published package; rollback anchor (1.18.1 environment + runtime pointer) recorded; zero-in-flight verified before pointer swap; clean service restart; health endpoint reports `ok`/1.18.2; import and CLI checks pass; doctor baseline unchanged from pre-release. |
| Runtime — second deployment host | Converged 2026-08-07T07:37:09Z | Same procedure and checks; rollback anchor retained; health `ok`/1.18.2; post-swap journal clean. |
| Runtime — third deployment host | Converged 2026-08-07T07:38:45Z | Same procedure and checks; pre-swap in-flight verified zero on both inbound sockets and upstream streams; health `ok`/1.18.2; post-swap journal clean. |
| Version floor | 1.18.2 | Floor raised after first-host convergence; all deployment hosts verified at or above the floor at closure. |

## Stage 6 — Watch period

- **Watcher:** TokenPak
- **Watch start:** 2026-08-07T02:45:39Z (first-host runtime convergence after
  publication)
- **Watch length:** 4 hours declared per deployment host; fleet-wide window
  closed early under the waiver recorded in Exceptions
- **Watch end:** 2026-08-07T07:48:13Z
- **Samples:**
  - First host: from publication (01:41Z) through 07:39Z, the only
    error-pattern journal lines are 19 upstream-stream teardown resets stamped
    in the single swap second 02:44:07Z (pooled/in-flight upstream connections
    severed by the service restart; durations attached to the resets confirm
    they were pre-swap streams). Zero error lines before or after that second.
    First-host clean runtime exceeded the declared four-hour window
    (02:45:39Z → past 07:39Z) before the remaining hosts converged.
  - Second host: pre-swap baseline hour showed zero proxy errors; the swap
    second logged 5 pooled upstream-stream teardown resets (durations
    1.9–32.1s); zero errors post-swap; health `ok`/1.18.2.
  - Third host: zero in-flight inbound sockets and upstream streams at swap;
    zero teardown noise; zero errors post-swap; health `ok`/1.18.2.
  - Fleet-wide convergence sample 2026-08-07T07:39:23Z: all three hosts
    health `ok`, runtime 1.18.2, floor 1.18.2.
- **Package downloads at closure (2026-08-07):** 45 last day; 476 last week;
  1,611 last month
- **Incoming user reports:** 0

Watch disposition:
**PASS WITH EARLY FLEET-WIDE CLOSE (see Exceptions).** The first deployed host
completed more than the declared four-hour window with zero non-teardown proxy
errors; the remaining hosts converged cleanly and their per-host windows were
closed early under the recorded waiver. Restart teardown resets at swap are
retained as evidence, not scrubbed; the pre-swap in-flight gate gains a
carry-forward improvement (count upstream-side streams, not only inbound
sockets — the third host already used the improved gate).

## Stage 7 — Closeout

- [x] Release marked successful
- [x] Changelog and shipped documentation reflect v1.18.2 behavior
- [x] Public source, tag, Release, PyPI, assets, and checksums agree
- [x] Applicable documentation, website, and registry surfaces are dispositioned
- [x] Every canonical host reports the 1.18.2 floor and passes health checks
- [x] Four-hour PATCH watch completed on the first deployed host; remaining
      per-host windows closed early under the waiver recorded in Exceptions
- [x] Independent closeout acceptance recorded
- [x] Follow-up work and retained limitations recorded

Closed by: TokenPak (release owner)

Closed at: 2026-08-07T07:48:13Z

## Exceptions / waivers

**Watch-window early close (governed waiver).** The declared four-hour
per-host watch completed in full on the first deployment host only. After
clean fleet-wide convergence samples on all three hosts, the release owner
explicitly directed early closure of the remaining per-host windows on
2026-08-07. Recorded here as a governed waiver rather than silently marking
the watch complete. Rollback anchors remain in place on every host after
closure.

No other exception: every required release channel, test, acceptance,
publication, propagation, deployment, and watch signal above was available and
is dispositioned in this record.

## Attachments / evidence

- Candidate head `df9f34d4a5f0a231acb701c10b307fdf852fdd27`; integration merge
  `88defbf7dfb492fccfcffca48c35d07d85de7461` (== public `main` == tag peel).
- Tag `v1.18.2`: annotated object by `TokenPak <hello@tokenpak.ai>`, tagged
  2026-08-07T01:26:44Z.
- Tag-push workflow run 31138043262 (green end to end);
  GitHub Release published 2026-08-07T01:41:01Z; PyPI files uploaded
  2026-08-07T02:38:25–27Z.
- Shipped artifact digests: wheel
  `b08d73aa8ae19f5f0d0bd856913fc336884416f796d7b9652a450aa06570ca18`, sdist
  `471fa32c0d20f1ec9e6fc503bf454747d41df024ce37b1b7524d6b4d41227d2a`.
- Documentation revision `8324009946dad49e336335731fe73915b0ef40d4`.
- Deployment receipts: three hosts converged (02:45:39Z / 07:37:09Z /
  07:38:45Z) with retained 1.18.1 rollback anchors; version floor 1.18.2.
- Watch samples as recorded in Stage 6.
- Independent exact-head acceptance (two rounds) and independent closeout
  acceptance recorded at closure.

## Retrospective

- **What dragged:** a GitHub Actions infrastructure outage stalled exact-head
  CI mid-flight (three runs cancelled, re-run to green), and fleet-wide
  runtime convergence trailed publication by roughly five hours awaiting
  deployment-host access.
- **What broke or nearly broke:** the structural safety scan flagged the
  product's own documented vault-index default (triaged false positive;
  pattern refinement follows separately); a post-publish version smoke read a
  stale version by running inside a source checkout (`sys.path` cwd
  shadowing) before being rerun from a neutral cwd; host swaps severed pooled
  upstream streams that the inbound-only in-flight gate did not count.
- **Carry-forward action:** refine the vault-path structural pattern; extend
  the pre-swap in-flight gate to count upstream-side streams on every host;
  advance the documentation revision pin at each release rather than
  deferring it; always run version smokes from a neutral cwd.
