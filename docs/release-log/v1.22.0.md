---
title: TokenPak v1.22.0 Release Log
version: 1.22.0
release_type: minor
semver_basis: backward-compatible fix restoring declared automatic context injection on non-byte-preserved routes, corrected vault-injection receipt accounting, and routine CI/SDK dependency refreshes
risk_class: full-conservative
changed_surfaces: python-core/proxy, workflow/release, packaging/dependencies, docs/public-claims
ci_selection_artifact: 49eab4f63eea610e70048d468b00abe66ab7bac9
status: complete
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-29T23:58:24Z
closed: 2026-08-31T07:19:43Z
---

# TokenPak v1.22.0 Release Log

## Scope

This minor release completes automatic context injection for OpenClaw/SDK
requests on non-byte-preserved routes. The call site that runs the
vault-injection stage on these routes landed previously but had no real
effect: it wrote to session accounting with an argument shape the current
writer rejects, and its adapter-detection fallback always resolved to a
no-op passthrough adapter regardless of the actual request. Both are fixed,
so declared route policy now reaches the request body the provider receives.

The release also fixes the proxy's vault-injection receipt: measured
injected-token counts and source names are now written to the live
per-server session that request accounting and `GET /stats` read, instead of
a compatibility global no live request path consults. `tokenpak status` now
reports real injection totals instead of zero across zero requests.

Five CI toolchain and SDK dependency pins are refreshed: `actions/setup-python`,
`actions/setup-node`, `pypa/gh-action-pypi-publish`, and the TypeScript SDK's
`axios` and `@types/node`.

The version-bearing candidate shipped as the immutable commit
`49eab4f63eea610e70048d468b00abe66ab7bac9`, tagged `v1.22.0`.
Candidate-wide validation and independent acceptance are complete; see
Release stages, below.

## Classification and compatibility

- **SemVer:** MINOR because the release adds one additive Python export (an
  explicit test seam) while preserving existing routes, defaults, and public
  symbols, alongside a behavior-restoring fix that lets already-declared
  route policy take effect for the first time.
- **Risk:** full-conservative because the accumulated delta crosses shared
  proxy request handling, session accounting, and CI/release workflow inputs.
- **Public API:** 4,698 symbols versus 4,697 in v1.21.0: one addition and
  zero removals. The addition is
  `tokenpak.proxy.vault_bridge.set_vault_index_override`, an explicit
  substitution seam for the vault index that replaces in-place
  module-attribute monkeypatching in tests.
- **Compatibility ruling:** retain the additive export in this candidate. No
  existing public symbol is removed or reclassified.
- **Breaking changes:** none identified.
- **Deprecations:** none introduced.

## User-visible behavior

- Routes whose policy declares automatic context injection now actually
  receive injected content in the request body sent upstream, instead of a
  silent no-op. The existing default-off master switch for the feature is
  unchanged by this release.
- The proxy's injection receipt (measured token count and source names) is
  now visible through `GET /stats` and `tokenpak status` instead of always
  reading zero.
- A malformed injection-pipeline result degrades to a measured zero at the
  receipt boundary instead of raising.

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.22.0"
```

No manual configuration migration is required. If a route's policy already
declared automatic context injection, upgrading changes that route's runtime
behavior from a silent no-op to active injection — review request-volume and
token-accounting expectations for those routes before upgrading.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.21.0"
```

No destructive state migration is introduced.

## Release stages

| Stage | Status | Evidence |
|---|---|---|
| Version-bearing surfaces | complete | Package version, machine metadata, API snapshot, changelog, release log, and exact-version documentation landed on the prepared candidate, commit `49eab4f63eea610e70048d468b00abe66ab7bac9`. Exact-candidate validation is complete. |
| Release-input acceptance | complete | All release-input PRs have exact-head independent acceptance; candidate-wide independent acceptance is also complete. |
| Release quality and artifacts | complete | Full-conservative validation, clean build, artifact inspection, clean install, upgrade, and rollback ran on the exact candidate. The release go/no-go review confirmed that the default `tokenpak --help` listing is a curated subset — the complete command set is shown by `tokenpak help --all` and in the command reference — and recorded a release-scoped exception for the difference; the default listing and the full reference remain distinct by design. |
| Independent release acceptance | complete | Independent acceptance recorded on the immutable release commit `49eab4f63eea610e70048d468b00abe66ab7bac9`. |
| Staging finalization | complete | Exact-head checks passed; staging pull request #751 (release branch) merged as `98215614b656796d71b2842baeb96ccc76d7afc1`. |
| Public promotion | complete | Governed fast-forward of the exact accepted commit to the public repository's main branch, 2026-08-30. |
| Tag, Release, and package publication | complete | Annotated tag `v1.22.0` pushed at `49eab4f63eea610e70048d468b00abe66ab7bac9`; the release workflow run succeeded; the GitHub Release was published with a checksums file, wheel, and sdist; PyPI `tokenpak==1.22.0` went live 2026-08-30. |
| Documentation, site, and runtime convergence | complete | The maintained hosts converged to 1.22.0 with rollback anchors recorded; a 24-hour watch (2026-08-30T07:19:43Z – 2026-08-31T07:19:43Z) closed clean — no restarts, zero request errors, no degraded status. Public documentation and release data converged to 1.22.0. |

## Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all technical gates pass on the exact release candidate? | Yes | The release workflow and all required staging checks passed on commit `49eab4f63eea610e70048d468b00abe66ab7bac9`; full candidate-wide validation is recorded. |
| 2 | Does staging validation have zero failed checks? | Yes | All required automated staging checks passed with zero failures. The release go/no-go review found that the top-level `tokenpak --help` listing was shorter than the documentation's canonical top-level command list, unrelated to this release's changes, and covered it under a recorded release-scoped exception (see Exceptions and waivers). |
| 3 | Are the release notes complete and reviewed? | Yes | The changesets are incorporated and independently reviewed. |
| 4 | Is rollback to v1.21.0 verified? | Yes | Clean-artifact upgrade and rollback ran on the exact candidate. |
| 5 | Are all breaking changes disclosed? | Yes | No breaking change is identified; the only public API change is one additive test-substitution export. |
| 6 | Is an active watcher assigned for publication? | Yes | An operator watched publication and the following 24-hour window; zero anomalies were recorded. |

Decision: **TAG.** All release-input and exact-candidate gates closed, with
one release-scoped exception recorded (see Exceptions and waivers); `v1.22.0`
was tagged at `49eab4f63eea610e70048d468b00abe66ab7bac9` and published.

## Known issues and limitations

- Automatic context injection remains governed by its existing default-off
  master switch; this release does not change that default.
- The default `tokenpak --help` listing is a curated subset of the
  available commands; the complete command set is shown by
  `tokenpak help --all` and in the command reference. The review recorded a
  release-scoped exception for the difference; the default listing and the
  full reference remain distinct by design. This is unchanged from the
  previously published release.

## Exceptions and waivers

- A release-scoped exception was recorded for this difference. It applied
  to this release's pre-publication validation and did not affect shipped
  behavior — it covered the default `tokenpak --help` listing being a
  curated subset of the available commands, distinct from the complete
  command set shown by `tokenpak help --all` and in the command reference.
  The exception was scoped to this release only; the default listing and
  the full reference remain distinct by design.

## Retrospective

- **What dragged:** The release go/no-go review, run in full for this
  release, found that the top-level `tokenpak --help` listing was shorter
  than the documentation's canonical top-level command list; confirming the
  gap predated this release and was not introduced by it took additional
  review before the release could proceed.
- **What broke or nearly broke:** Nothing shipped-behavior broke; the gap
  found was confirmed identical to the previously published release, not
  caused by this release's changes.
- **Carry-forward action:** Keep the command reference and the
  `help --all` listing in step; state the curated default explicitly in
  the reference.
