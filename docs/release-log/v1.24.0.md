---
title: TokenPak v1.24.0 Release Log
version: 1.24.0
release_type: minor
semver_basis: a savings-led dashboard information-architecture restructure, a directly-importable retry-primitive module, first published time-remaining forecast calibration data for one cell, a compatibility-preserving internal proxy-launcher relocation, a request-accounting fix for bodyless model requests, a `tokenpak serve` port-precedence fix, a development-dependency extras expansion, and a dependency-lockfile security refresh
risk_class: full-conservative
changed_surfaces: python-core/proxy, python-core/cli, python-core/contracts, dashboard, docs/public-claims, packaging/dependencies, release-pipeline
ci_selection_artifact: 347e234e5d663d1421cb9876aca198fbe4a0c9c7
status: complete
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-09-03T00:00:00Z
closed: pending
---

# TokenPak v1.24.0 Release Log

## Scope

This minor release restructures the served local dashboard around a
savings-led, seven-block information hierarchy: a savings hero, a combined
health status strip, a compression chart, a cache strip that separates
product-attributed from provider-level cache hit rate, a recent-requests
table, a mode/session breakdown, and quick actions. Backend support is
additive (new `GET /savings` and `GET /recent` endpoints, a new
`window_24h` field on two existing endpoints) and fails open when the
request log is unavailable.

The release publishes the first reviewed time-remaining forecast
calibration cell (`claude-sonnet-5` / unknown effort / streaming) into the
per-cell publication table added in the prior release. The mechanism
remains gated behind its existing default-off master switch; populating the
table makes the cell eligible, it does not change the shipped default.

The retry engine and its supporting types are now directly importable from
a new `tokenpak.core.retry` module; the previously documented import path
continues to work unchanged as a compatibility re-export. An internal
proxy-launcher module is relocated from `tokenpak.core.runtime.proxy` to
`tokenpak.proxy.bootstrap`; the externally documented compatibility import
path (`tokenpak.runtime.proxy`) is unaffected, and a lazy compatibility
shim at the exact old internal path keeps it importable too (resolved on
first attribute access, so it never eagerly imports the proxy layer) —
this closes the public-API-snapshot question this release's own
pre-cut review surfaced, without a declared removal.

A request-accounting fix stops a model-endpoint request sent with no body
from logging a spurious internal error after the response has already
reached the client; usage for such requests is now recorded normally. The
`dev` extra is expanded to pull in the serving-surface dependencies so a
plain development install can import, test, and strictly type-check the
proxy and dashboard subsystems.

A dependency-lockfile refresh closes several open security advisories:
`aiohttp`, `cryptography`, and `h2` (core runtime dependencies — the pinned
lockfile was stale, though a fresh unpinned resolve already picked up
patched releases) and `nltk` (optional `compression`/`llamaindex` extras
only; one nltk advisory, CVE-2026-81726, has no upstream fix yet and
remains open for anyone using those extras). Two npm lockfiles pick up
dev-tooling-only bumps (`browserslist`, `js-yaml`, and an incidental
`brace-expansion` bump), none of which reach either published package's
runtime dependency graph.

**A first candidate for this release was prepared and then withdrawn before
publication.** A full staging-validation walkthrough on that candidate
found that `tokenpak serve` silently ignored the configured port
(`~/.tpk/config.yaml`'s `port:` key) and the `TOKENPAK_PORT` environment
variable whenever the `--port` command-line flag was not also given,
instead of honoring them as documented. That defect was pre-existing on
the prior shipped version as well, not introduced by this release's
content, but it was newly confirmed and recorded during that walkthrough
and blocked the first candidate from proceeding. A fix was developed,
reviewed, and merged (see Fixed, below) before this candidate was
prepared; this candidate includes that fix. It is the second candidate
prepared for this release, and it is the one published as TokenPak
v1.24.0.

Thirteen further merges (this release's own bandit-baseline, reproducible-
build-ownership, and strict-mypy typing work; a stale-test repair; two
release-log documentation finalizations for prior versions; and six
routine dependabot bumps) are internal or documentation-only, with no
changelog entry.

## Content

First-parent merges on `github-staging/main` since the v1.23.0 release cut
(`f3fd1cda4f618344f0c7aca6686a061bb841faa3`) through this candidate's
parent (`e9d6001d1960cdb6f704b386f42dd092f76ac375`):

| PR | Merge SHA | Item | Changelog |
|---|---|---|---|
| #764 | `1c4b2688fb` | CI: bandit baseline | internal, no entry |
| #765 | `f5058c5bd7` | Reproducible build ownership (release-gate normalize scripts, umask/owner) | internal, no entry |
| #766 | `6ded2de8c7` | Time-band forecast cell first publication | `[1.24.0] Added` |
| #767 | `7598ff6385` | Dashboard seven-block information hierarchy | `[1.24.0] Changed` |
| #768 | `dbe1b920ec` | Typing: session-forecast strict-mypy | internal, no entry |
| #769 | `538c902f9b` | Build: `dev` extra includes `tokenpak[serve]` | `[1.24.0] Dependencies` |
| #770 | `35a6bb40b3` | Typing: proxy-server residual strict-mypy | internal, no entry |
| #771 | `eec2d93faf` | Fix: proxy empty-body usage observation | `[1.24.0] Fixed` |
| #772 | `3065886c99` | Test: savings-display stale tests | internal, no entry |
| #773 | `8dec181398` | Retry primitive relocated to `tokenpak.core.retry` (new module; old path retained as compatibility re-export) | `[1.24.0] Added` |
| #774 | `1427c72177` | Proxy launcher relocated `tokenpak.core.runtime.proxy` → `tokenpak.proxy.bootstrap` | `[1.24.0] Changed` |
| #775 | `0672cc8bd5` | Docs: release-log v1.23.0 final | docs only, no entry |
| #776 | `86f662acb0` | Docs: release-logs v1.21.0–v1.22.0 final | docs only, no entry |
| #756–761 | 6 SHAs | Dependabot bumps (build, `@types/node` ×2, jest, ruff, dawidd6/action-download-artifact, setup-go) | routine dependency, no entry |
| #778 | `8ba4fcc9bd` (head `a813b8a3b7`) | Compatibility: lazy shim keeps `tokenpak.core.runtime.proxy` importable | `[1.24.0] Changed` |
| #777 | `31d51f464c` (head `11362d103d`) | Dependency/security lockfile refresh (nltk, aiohttp, cryptography, h2; browserslist, js-yaml, brace-expansion) | `[1.24.0] Security` |
| #779 | `e9d6001d19` (head `c5fa129c51`) | Fix: `tokenpak serve` honors configured port / `TOKENPAK_PORT` when `--port` is absent | `[1.24.0] Fixed` |

## Sign-offs

- PR #778 (`compat/core-runtime-proxy-lazy-shim`, head `a813b8a3b7`) —
  accepted by a distinct captain. Evidence recorded at acceptance: 13-name
  agreement across the shim's `__all__`, `tokenpak.proxy.bootstrap`, and
  the regenerated snapshot; import-linter edge counts unchanged (43/36/11)
  confirming no static/eager import was reintroduced; the snapshot
  generator resolves the lazily-declared `__all__` names, so the 13 rows
  persist on regeneration; strict mypy clean across 284 files; CI 50/50.
  Sequenced first.
- PR #777 (`deps/security-lockfile-refresh`, head `11362d103d`) — accepted
  by a distinct captain. Evidence recorded at acceptance: four pip pins
  moved within their already-declared ranges, `uv.lock` byte-deterministic
  at the base commit; npm bumps confirmed dev-only in both lockfiles;
  `pip-audit` and `npm audit --omit=dev` clean; CI 41/41. A non-blocking
  disclosure gap was noted at acceptance — the `packages/tokenpak-js`
  lockfile's incidental `brace-expansion` bump was not named in the PR's
  own changelog line — closed in this release's `[1.24.0]` Security entry.
  Sequenced second.
- PR #779 (`fix/serve-port-precedence`, head `c5fa129c51`) — accepted by a
  distinct captain. Evidence recorded at acceptance: precedence/help/
  config-key/error path verified by reading; CLI reference regeneration
  reproduced (empty diff); 11/11 regression tests, fail-before proven at
  base; four live smoke scenarios reproduced; strict mypy clean across 284
  files; CI 50/50 (job logs: 8599/8629/8629/8629 passed, 0 failures). A
  non-blocking note: the PR body cites a local test count (8621) that
  matches no CI job log; this release log does not repeat that figure.
  Sequenced third, after the first candidate was withdrawn for the defect
  this PR fixes (see Scope, above).
- All three PRs' own review and CI evidence is summarized above from their
  acceptance records; this log does not re-run or re-litigate any of their
  review.

## Classification and compatibility

- **SemVer:** MINOR — every content change in this release is additive
  (new module, new calibration data, new endpoints/fields, two bug fixes, a
  dependency-extras expansion) or a same-object relocation behind an
  unaffected compatibility path, including the previously open internal
  `tokenpak.core.runtime.proxy` question, now closed by a lazy
  compatibility shim rather than a declared removal (see Public API,
  below). This release is versioned explicitly as `v1.24.0`, classified
  MINOR under this project's release-classification policy.
- **Risk:** full-conservative — the accumulated delta touches core proxy
  request handling, the CLI-adjacent contract surface, and the dashboard,
  in addition to public documentation.
- **Public API:** confirmed by a CI-parity regeneration on the exact
  release candidate (see Release stages, below). All 13
  `tokenpak.core.runtime.proxy.*` symbols present in the v1.23.0 snapshot
  remained present, unchanged, in the regenerated snapshot — the lazy
  compatibility shim landed in #778 resolves them on first access, so the
  release-gate snapshot check (`make release-gate-check`) passed clean
  with zero undeclared removals. The one navigation-entry change (the
  `proxy` submodule ceasing to be listed in `tokenpak.core.runtime`'s own
  `__all__`) is the expected, self-consistent consequence of the same
  relocation.
- **Compatibility ruling:** retain — no public symbol is removed relative
  to v1.23.0. The one navigation-entry change is internal bookkeeping (a
  submodule ceasing to be listed in its parent's `__all__`), not the loss
  of an importable name; the submodule `tokenpak.core.runtime.proxy` and
  all 13 of its names remain importable via the lazy shim.
- **Breaking changes:** none, confirmed on the exact candidate tree (see
  Public API, above).
- **Deprecations:** `tokenpak.core.runtime.proxy` now emits a
  `DeprecationWarning` on first access, directing callers to
  `tokenpak.proxy.bootstrap`; this is a new deprecation warning, not a
  removal.

## User-visible behavior

- The served dashboard (`/dashboard`) has a new layout: a top-of-page
  savings figure, one combined health signal in place of the previous
  status dot, a compression chart, a cache strip that no longer derives its
  headline number from proxy uptime, a recent-requests table, and the
  existing mode/session breakdown and quick actions. Client-side refresh
  interval changes from 30s to 5s and pauses while the browser tab is
  backgrounded.
- Session-economics payloads gain reviewed time-remaining data for one
  measured cell (`claude-sonnet-5`, unknown reasoning effort, streaming);
  every other cell continues to report no data available. The feature's
  master switch remains default-off.
- The retry engine is now directly importable from a shorter, discoverable
  module path (`tokenpak.core.retry`); the previously documented import
  path is unaffected.
- A model-endpoint request sent with no body no longer produces a spurious
  internal log entry; its usage is now recorded like any other request.
- `tokenpak serve` now honors the configured `port` (`~/.tpk/config.yaml`)
  and the `TOKENPAK_PORT` environment variable when `--port` is not given,
  instead of always binding the built-in default. Precedence: `--port`
  flag > `TOKENPAK_PORT` env var > config file `port` > built-in default.
- A plain development install (`pip install -e ".[dev]"`) now includes the
  dependencies needed to import and test the proxy and dashboard code
  paths, without a separate extras flag.
- No user-visible runtime behavior change from the dependency-lockfile
  refresh; it closes advisory exposure in pinned installs only.

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.24.0"
```

No manual configuration migration is required. The new calibration data and
the retry/proxy-launcher relocations are inert with respect to runtime
behavior. Anyone who was relying on `tokenpak serve`'s previous
port-precedence defect (configured port silently ignored) will now see the
documented order honored instead — no other default changes result from
upgrading.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.23.0"
```

No destructive state migration is introduced.

## Release stages

| Stage | Status | Evidence |
|---|---|---|
| Version-bearing surfaces | complete | `version.json`, `tokenpak/__init__.py`, nine documentation example literals, `CHANGELOG.md`, and this release log landed on the release branch, commit `347e234e5d663d1421cb9876aca198fbe4a0c9c7` (tree `fec6978992624cdad732161b7529345a652d66b4`, sole parent `e9d6001d1960cdb6f704b386f42dd092f76ac375`), the second cut prepared for this release. The public-API snapshot regeneration and build ran clean on this exact commit. |
| Release-input acceptance | complete | All 16 non-dependabot merges since the v1.23.0 cut (see Content, above) carry their own PR-level review; #778, #777, and #779 additionally carry independent-captain acceptance records reproduced under Sign-offs, above. This log does not re-run or re-litigate any individual PR's own review. |
| Release quality and artifacts | complete | Staging validation on the exact candidate found zero failed checks: full regression suite 8621 passed, 373 skipped, 108 deselected, 2 xfailed (exit 0); benchmark −0.32%; strict typing clean across 284 files; bandit reported 0 new Medium/High findings; import-linter contracts held at 3. Rollback to v1.23.0 was exercised (wheel install into the upgraded environment plus reopen checks) and passed. The port-precedence defect confirmed in the withdrawn first candidate was confirmed fixed live (`port:` in config and `TOKENPAK_PORT` both honored). Clean-machine pipeline timing: 39.8s median versus 40.6s for v1.23.0 on an idle host — no regression. The wheel and sdist rebuilt in two independently prepared build environments (different umask) were byte-identical, and the published bytes on PyPI and the GitHub Release both byte-match the pre-registered digests — the first release for which this holds; the owner/group and umask divergence recorded in the v1.23.0 release log is closed by this release's normalization fix. |
| Independent release acceptance | complete | The registered validation program's three rounds (two executor rounds plus an artifact-partition round) all passed exactly once, with no retries, and the program accepted the candidate. Independent acceptance is recorded on the immutable release commit `347e234e5d663d1421cb9876aca198fbe4a0c9c7`. |
| Staging finalization | complete | Exact-head checks passed; staging pull request #780 (release branch, second candidate cut) independently accepted and merged as `70c6a325b0e05b5428d182f1926725849278fe6d`. |
| Public promotion | complete | Governed fast-forward of the exact accepted commit — a true fast-forward, `f739654ef6..347e234e5d` — to the public repository's main branch; annotated tag `v1.24.0` pushed at that commit, tagger `TokenPak <hello@tokenpak.ai>`. |
| Tag, Release, and package publication | complete | Release workflow run `33795409285` (tag push) completed with all jobs green. PyPI `tokenpak==1.24.0` went live 2026-09-03 (wheel uploaded 19:26:43Z, sdist 19:26:45Z). The GitHub Release `v1.24.0` was published 2026-09-03T19:27:06Z with a wheel, sdist, and `SHA256SUMS`. Artifact digests: wheel `9d1214f03fc7d9968852d93089a5b680b126b1f7875063722b289cebdea874f4`, sdist `4bd8a90ef4c8517caeeb7d0818483049727a906d45e442ca29bf809058593633`; the GitHub Release checksums, the PyPI-declared digests, and independently downloaded bytes all agree. |
| Documentation, site, and runtime convergence | complete — watch open | Public documentation converged at `tokenpak/docs` commit `f73ce7c2ab99776adaf8c672bf3d9c2c6e20d352` (Pages run `33796831405`); site release data converged at `tokenpak/site` commit `b39b0e495fb4912437bd354eff9191c618d2341e` (sync run `33797254083`, deploy run `33797293866`). The package registry has no version-pinned surface, so no change was required there. All three managed runtime hosts upgraded from v1.23.0 to v1.24.0 with health checks passing; deploy completed 2026-09-03T19:37:30Z. The 24-hour post-deploy watch is open through 2026-09-04T19:37:30Z; closeout follows once it completes clean. |

## Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all technical gates pass on the exact release candidate? | Yes | The release-gate snapshot regeneration and check, full-conservative validation, and build all passed on commit `347e234e5d663d1421cb9876aca198fbe4a0c9c7`. |
| 2 | Does staging validation have zero failed checks? | Yes | The first candidate for this release failed this question (`tokenpak serve` port-precedence defect); the fix landed on trunk (#779) before this candidate was cut. A fresh staging-validation walkthrough on this exact candidate recorded zero failed checks (see Release stages, above). |
| 3 | Are the release notes complete and reviewed? | Yes | The `[1.24.0]` `CHANGELOG.md` section is grounded against source diffs and was independently reviewed as part of candidate acceptance. |
| 4 | Is rollback to v1.23.0 verified? | Yes | A wheel install of v1.23.0 into the upgraded environment, followed by reopen checks, passed on the exact candidate. |
| 5 | Are all breaking changes disclosed? | Yes | None identified. The public-API snapshot regeneration confirmed all 13 `tokenpak.core.runtime.proxy` rows remain present via the lazy compatibility shim; see Classification and compatibility, above. |
| 6 | Is an active watcher assigned for publication? | Yes | TokenPak is the release owner and remains the active watcher through the 24-hour post-deploy window (open through 2026-09-04T19:37:30Z). |

Decision: **TAG — cut and published.** All release-input and exact-candidate
gates closed; `v1.24.0` was tagged at
`347e234e5d663d1421cb9876aca198fbe4a0c9c7` and published to PyPI and the
GitHub Release on 2026-09-03T19:27:06Z. The 24-hour post-deploy watch remains
open through 2026-09-04T19:37:30Z; closeout follows once it completes.

## Known issues and limitations

- One nltk security advisory, CVE-2026-81726, has no upstream-patched
  release as of this lockfile refresh. It affects only the optional
  `compression` and `llamaindex` extras (nltk is not part of the base
  install); it remains open and is not fixable by this or any version bump
  alone.
- Documentation content for `tokenpak cost --json` and the remaining
  `TOKENPAK_COMPACT` "master switch" wording in three public documentation
  pages (outside this repository) were carried forward as known issues in
  the v1.23.0 release log. No merged content in this release's own inputs
  addressed either item, and this log does not independently confirm their
  current status in the documentation repository; both remain open and
  carry forward to the next release.
- The local dashboard's landing-page layout and empty-state messaging,
  carried forward as an unaddressed item in the v1.23.0 release log, were
  restructured by this release (see Scope, above). The served dashboard
  template and its backend data sources were read against the savings-led
  information-hierarchy content and every required element was present;
  this item retires with this release and does not carry forward.
- The time-remaining forecast mechanism remains governed by its existing
  default-off master switch and now-partially-populated per-cell
  publication table; this release adds one cell's data but does not flip
  the switch. Populating further cells or flipping the switch is separately
  gated and not authorized by this release.
- 33 open Dependabot security advisories across the public and staging
  repositories (17 and 16 respectively, including one critical each) were
  recorded as a standing condition ahead of this release's cut and were not
  triaged against this release's content; none has an open pull request
  coupling it to this release.

## Exceptions and waivers

None.

## Retrospective

- **What dragged:** The first candidate for this release was withdrawn
  after a full staging-validation walkthrough found that `tokenpak serve`
  silently ignored the configured port and `TOKENPAK_PORT` whenever
  `--port` was not given. The defect was pre-existing on the prior shipped
  version, not introduced by this release's own content, but it blocked
  the first candidate from proceeding. The fix (#779) landed on trunk
  before the second candidate — the one published as v1.24.0 — was cut.
- **What broke or nearly broke:** Nothing broke. Published bytes on PyPI
  and the GitHub Release byte-matched the pre-registered reproducible-build
  digests exactly — the first release for which this holds, closing the
  tar owner/group and umask mode-bit divergence recorded as an accepted
  finding in the v1.23.0 release log.
- **Carry-forward action:** The open nltk advisory (CVE-2026-81726, no
  upstream fix), documentation content for `tokenpak cost --json` and the
  remaining `TOKENPAK_COMPACT` wording in three public documentation pages,
  and the standing set of open Dependabot security advisories across the
  public and staging repositories all carry forward to the next release.
