---
title: TokenPak v1.29.0 Release Log
version: 1.29.0
release_type: minor
semver_basis: additive authenticated native-token observation API
risk_class: consequential
validation_cadence: compact-minor-with-focused-correctness
changed_surfaces: token-accounting, storage, companion-hooks, packaging
status: in-progress
owner: TokenPak
started: 2026-09-21
closed: pending publication
---

# TokenPak v1.29.0 Release Log

## Native token observations and first-session reliability

This candidate adds opt-in, authenticated native token observations with explicit
request coverage, bounded reservations and response-completion evidence. Token
counts remain separate from billed cost, calibrated forecasts and accepted task
outcomes. Additive database migrations preserve historical accounting evidence.

Fresh companion journals now record the first Claude prompt when an external
SQLite executable is absent. The managed hook uses the selected interpreter and
journal directory. The MCP server reports startup and malformed-input diagnostics
on stderr while keeping JSON-RPC on stdout. Dispatch help describes its optional
dependencies and the remaining station execution and delivery limitations.

## Upgrade and rollback

The candidate pair is OSS 1.29.0 and Pro 0.5.0, using TIP-1.0. Pro 0.5.0 declares
OSS 1.26.0 through 1.29.0 compatibility; native-token measurement needs OSS 1.29.0.
Pro 0.4.3 supports OSS only through 1.28.0. Upgrade the packages together once
both releases are published through their existing distribution channels.

Back up configured local databases and configuration before upgrading. The
database changes are additive and do not relabel historical rows as native
measurements. Schema migration is one-way. Rollback restores OSS 1.28.0 and Pro
0.4.3 together with the pre-upgrade database backup. Preserve a separate copy of
post-upgrade state before restoring; do not delete new evidence or try to reverse
the schema migration in place.

## Validation and open requirements

Candidate wheel and source builds pass package metadata checks. An isolated
installation of OSS 1.28.0 / Pro 0.4.3 was upgraded to the candidate pair, rolled
back with its pre-upgrade backup, and upgraded again. Configuration, journal
entries and historical accounting rows were preserved; new state was retained
separately during rollback. Missing licenses still refuse entitled commands.

The migration verifier now creates each historical schema atomically, avoiding
per-statement disk commits while preserving its DDL restrictions. Its 30 focused
checks pass with the existing timeout, including rollback after a rejected
declaration. Current-version documentation examples were also corrected.

This is release preparation, not a published release or a deployment receipt.
A fresh September 21 audit covered all 213 locked package/version
pairs and 39 resolved release-tool dependencies, with no yanked files. The two
open findings affect optional NLTK and Accelerate dependencies; both receive
High treatment under CVSS v3.1. This release accepts those findings for this
release only with the limitations in [SECURITY.md](https://github.com/tokenpak/tokenpak/blob/main/SECURITY.md) and the
[changelog](https://github.com/tokenpak/tokenpak/blob/main/CHANGELOG.md#1290--2026-09-21). The audit is not clean, and the
previous release's acceptance is not carried forward. The standard installed
candidate pair excludes both affected packages and their optional consumers.

Real provider outcomes, per-model calibration, human comprehension and customer
retention remain separate requirements. This release does not establish billed
subscription savings or enable automatic rerouting. Payments and self-service
checkout are outside this candidate.

## Pre-cut acceptance

| Check | Completed evidence |
|---|---|
| Source and focused checks | [Candidate PR841](https://github.com/tokenpak/tokenpak-staging/pull/841) passed all 50 hosted checks. The six-release migration gate passed 18 store/baseline cases; 30 migration tests and 97 documentation/version checks passed. |
| Paired Pro | Pro 0.5.0 passed 1,675 tests on each of Python 3.10, 3.11 and 3.12, including exact producer binding. Public producer rebinding and fresh owning CI remain mandatory before Pro integration. |
| Disclosure correction | [PR842](https://github.com/tokenpak/tokenpak-staging/pull/842) head `697385e8f3c9c87f80797ec0c2718d7c81911935` passed all 12 emitted workflows: 28 successful checks and nine classifier-owned skips. Its owning CI workflow does not select these five documentation paths. Strict documentation, CLI reference and 91 documentation/version checks pass locally. |
| Artifact and installation | Rebuilt wheel/sdist pairs pass Twine. Fresh installed CLI, MCP, first-prompt journal, package compatibility and dependency checks pass. An isolated proxy starts and serves health and session economics with truthful unavailable states and no provider calls. Final wheel payloads match the previously upgrade-tested pair. |
| Upgrade and rollback | Published 1.28.0 / Pro 0.4.3 upgraded, rolled back with the pre-upgrade backup and upgraded again. Configuration, journal and historical accounting survived; post-upgrade state was preserved separately. |
| Security and identity | The fresh dependency review and release-specific exception are disclosed above. The checked standard installation excludes the affected optional packages. Source ancestry, canonical feature identities and existing staging merge-node provenance were verified. |
| Minimal-install rehearsal repair | The previous public version's September 14 and 21 rehearsals failed because archive tests confused a generated `build/` namespace with the optional build frontend. The original collision is reproduced, the corrected dependency check skips it, and all 19 metadata tests pass with the real build frontend, including wheel and sdist inspection. A minimal interpreter passes 17 checks with the two dev-only archive checks explicitly skipped. Fresh hosted rehearsal validation remains required before tagging. |
| Prompt response isolation | A pending SQLite writer held response pipes open in both the public base and previous candidate. A controlled blocked-writer fixture reproduces both cases; the correction returns the prompt before the writer is released and keeps writer output private. Hook, fallback and launcher checks pass 60 tests with one existing skip on complete Python 3.10 and 3.12 environments. The 50 ms latency assertion is unchanged. |

The first disclosure correction failed strict documentation build because two
relative links pointed outside the documentation tree. The corrected links pass;
the original failed run is retained. The broad navigation audit's orphan-page
warnings are unchanged from the base, and its release-gate mode passes. The
initial local CLI-doc check lacked its selected Python environment; the corrected
invocation passes without changing the check.

The previous public candidate's Python 3.10 latency check failed at 127.7 ms.
That failure remains blocking for its source; no unchanged rerun is substituted.
The pipe-lifetime defect is independently reproduced on both old sources, not
claimed as a complete explanation of the hosted timing failure. An initial local
Python 3.10 check also lacked launcher dependencies and exceeded the timing bound;
the complete, selected-interpreter environment passes the affected checks.

The final correction closes the SQLite writer's inherited standard descriptors
and adds the deterministic regression check. Fresh exact-head CI,
source/archive and artifact binding are enforced before public promotion and
tagging. Earlier hashes are evidence for their named inputs, not final publisher
artifact digests. Broad first-use assessment remains deferred; actual customer
comprehension, calibration and outcomes are still unmeasured.

## Cut-time decision

| Question | Answer |
|---|---|
| Do applicable technical gates pass? | Yes for the reviewed source, artifact and evidence scopes above, with the disclosed optional-dependency exception. Final-source enforcement remains mandatory. |
| Are selected staging checks unresolved? | No for the accepted heads above; original failed results are retained. The final prompt-response correction must pass fresh owning CI and affected installed-artifact checks before promotion. |
| Are release notes complete and reviewed? | Yes: scope, limitations, paired upgrade, additive migrations, rollback and security findings are disclosed. |
| Is rollback verified? | Yes, using the previous published pair and the untouched pre-upgrade state backup. |
| Are compatibility changes disclosed? | Yes: opt-in additive native-token observations, preserved historical rows, TIP-1.0 and the explicit paired compatibility range. |
| Is monitoring owned? | The release owner is watching this operation. Active ownership is rechecked before publication; deployment starts fresh prospective observation and does not reuse historical coverage. |

Decision: **READY TO CUT**, reviewed by the TokenPak release owner on
September 22, 2026. This records source readiness; it does not assert publication,
Pro integration, deployment or closeout.

| Post-cut stage | State |
|---|---|
| Public promotion, tag and package publication | pending publication |
| Published artifact and clean-install readback | pending publication |
| Documentation, site and runtime convergence | pending publication |
| Prospective observation and release closeout | pending publication |
