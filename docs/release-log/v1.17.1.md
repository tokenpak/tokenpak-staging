---
title: TokenPak v1.17.1 Release Log
version: 1.17.1
release_type: patch
status: in-progress
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-03T04:30:00Z
closed:
---

# TokenPak v1.17.1 Release Log

## Scope

This patch release fixes proxy, configuration, and launcher paths and makes the
TIP v1 schemas citable at stable canonical URLs. It also retires a historical
changeset-note backlog (27 notes describing changes shipped in v1.7.1 through
v1.17.0) with no code effect.

Linked work:

- [OpenAI base-URL `/v1` documentation fix](https://github.com/tokenpak/tokenpak-staging/pull/662)
- [TIP v1 schemas as citable versioned artifacts](https://github.com/tokenpak/tokenpak-staging/pull/660)
- [Spend-guard state-lock contention fix](https://github.com/tokenpak/tokenpak-staging/pull/657)
- [Home-toggle resolution fix with restored coverage](https://github.com/tokenpak/tokenpak-staging/pull/663)
- [Launcher fallback-hook isolation](https://github.com/tokenpak/tokenpak-staging/pull/678)

## Sign-offs

| Role | Name | Decision | Timestamp |
|---|---|---|---|
| Release owner | TokenPak | Approved the patch wave and this pre-tag record; tag and publication remain separately gated | 2026-08-03T05:30:00Z |
| Independent reviewer | TokenPak | Every wave change carried exact-head CI green on its updated head before its guarded merge | 2026-08-03T05:30:00Z |
| Rollback decider | TokenPak | Retains rollback authority through the publication watch | 2026-08-03T05:30:00Z |

## Stage 0 — Change readiness

- Five wave changes merged serially into the integration trunk, each with a full
  branch update and exact-head CI pass (32-check board, zero adverse) before a
  head-guarded merge. Four changes classified as minor-worthy were deferred out
  of this patch train (feature surface, new CLI verb, and a public-symbol
  removal) and are recorded for a future minor release.
- Version surfaces: `__version__` 1.17.1; `version.json` 1.17.1; changelog entry
  present; the public-API snapshot regenerates in a full-extras parity
  environment and must show `package_version` 1.17.1 with no unexplained symbol
  delta before the release candidate is accepted.

## Stage 1 — Staging preparation

- **Release-content base:** `26c4aa805da22242f7168bd4ead267d247563de9`
- **TestPyPI candidate:** stable metadata `1.17.1`; the test-index route remains
  unestablished host-wide — if still absent at the pre-publish gate, the recorded
  substitute local-artifact gate applies (see Exceptions / waivers).

## Stage 2 — Staging validation

Executed 2026-08-03 on the release candidate (patch subset):

| Check | Result |
|---|---|
| Wheel build from the candidate tree | Pass (exit 0, single expected wheel) |
| Clean-venv install of the built wheel | Pass; `--version` reports 1.17.1 |
| Diagnostics (`doctor`, isolated environment) | Pass (exit 0) |
| Demo path | Pass (exit 0, savings output present) |
| Upgrade 1.17.0 → 1.17.1 with existing state | Pass; state opens, CLI works |
| Rollback → 1.17.0 | Pass; previous CLI verified |
| API snapshot parity | Pass: full-extras parity environment, 4,626 records, the four known framework-example sentinels only, version-only delta |

Patch-subset omissions, covered elsewhere: sdist build and the multi-Python
install matrix run in the tag-triggered release workflow; the full test suite,
proxy flows, and byte-fidelity ran green in each wave change's exact-head CI.

## Stage 3 — Go / no-go

| # | Question | Answer | Notes |
|---|---|---|---|
| 1 | Do all technical gates pass on the release commit? | At the tag gate | Exact-head CI, artifact integrity, and snapshot parity must be green on the final candidate. |
| 2 | Does staging validation have zero failed checks? | At the tag gate | Stage 2 must complete with zero failures. |
| 3 | Are the release notes complete and reviewed? | At the tag gate | This record and the changelog entry complete pre-tag notes; exact-head acceptance is required before landing. |
| 4 | Is rollback known to work for this release? | At the tag gate | Rollback to 1.17.0 must be executed and verified before tagging. |
| 5 | Are all breaking changes disclosed? | Yes | No breaking change or deprecation is intended; no public symbol is added or removed by this wave. |
| 6 | Is an active watcher assigned? | Yes | TokenPak owns the publication watch beginning after verified publication. |

Decision: **GO only when every binding precondition is verified on the final exact
source and artifact pair. Until then, NO TAG.**

## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.17.1"
```

No data migration is identified.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.17.0"
```

Published artifacts are immutable; a production problem is handled through
rollback, yank, or a superseding version, never by moving the released tag.

## Stage 4 — Production deploy

| Substep | Timestamp | Output / URL |
|---|---|---|
| Final public source freeze | | |
| Tag push | | |
| Build | | |
| GitHub Release | | |
| Package publication | | |
| Package-index verification | | |
| Clean-install verification | | |
| Documentation and website propagation | | The canonical TIP schema URLs (`/schemas/tip/…`) must resolve on the live documentation site before closeout |

## Stage 5 — Post-deploy validation

Pending production publication; runs from a fresh install from the production
package index.

## Stage 6 — Watch period

- **Watcher:** TokenPak
- **Watch start:** the verified publication timestamp
- **Watch length / end:** per release class, or as the release owner directs,
  recorded here either way

## Stage 7 — Closeout

- [ ] Release marked successful
- [ ] Shipped documentation reflects v1.17.1 behavior
- [ ] Changelog entry shipped as written
- [ ] Public release surfaces show v1.17.1
- [ ] Canonical TIP schema URLs resolve on the live documentation site
- [ ] Follow-up issues recorded
- [ ] Release communications recorded
- [ ] Watch period completed as recorded

Closed by: Pending

Closed at: Pending

## Exceptions / waivers

The test-index (TestPyPI) route remains unestablished host-wide; establishing it
is tracked follow-up infrastructure work. If it is still absent at this release's
pre-publish gate, the substitute local-artifact gate (clean install, version and
diagnostics checks across supported Python versions, upgrade, and rollback on the
exact artifacts) applies, recorded as a deviation exactly as in the previous
release.

## Attachments / evidence

- Staging wave pull requests linked above; publication evidence recorded at
  Stage 4 during the release.

## Retrospective

- **What dragged:** Pending closeout
- **What broke or nearly broke:** Pending closeout
- **Carry-forward action:** Pending closeout
