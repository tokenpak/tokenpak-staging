---
title: TokenPak v1.18.3 Release Log
version: 1.18.3
release_type: patch
status: complete
owner: TokenPak
reviewer: TokenPak
rollback_decider: TokenPak
started: 2026-08-07T18:20:00Z
closed: 2026-08-07T21:20:00Z
---

# TokenPak v1.18.3 Release Log

## Scope

This compatible patch reduces the token overhead the companion itself adds to
agent sessions: guidance surfaces (generated Codex AGENTS.md section, Claude
launcher prompt fragment, MCP tool descriptions, companion guide) stop
directing agents to spend model round-trips on accounting that hooks already
perform out-of-band; `estimate_tokens` MCP results are compacted to the fields
an agent acts on while keeping a short estimator disclosure; and the `lean`
companion profile advertises only core MCP tools in `tools/list` while
dispatch stays unfiltered. It also makes the companion session label plain
text (robust to host-side control-byte sanitization) and hardens the delta
leak gate, private-path scan, and release workflow quoting.

Out of scope: pricing, billing, storage migrations, TIP wire changes, any
wire-mutating request optimization (separately gated design work), and the
open dependency-update wave (deferred with reason in the captain packet).

Classification note: the release adds one additive public symbol
(`active_tools` in the companion MCP package, re-exported by its server
module). A strict SemVer reading of an added public symbol indicates MINOR;
the operator explicitly classified this release patch-class with that
evidence on record (designation and verbatim authorization text recorded in
internal release governance, 2026-08-07). The tension is recorded here and
in the governance record rather than resolved silently.

Changelog reconciliation performed at cut: the pak canonical-schema entries
(`pak create` schema_version 2, `pak migrate`, legacy readability) were found
in [Unreleased] while their code is reachable from the v1.18.2 tag — a
v1.18.2 changelog under-report. They were relocated into the 1.18.2 section
as a marked addendum, not re-shipped under 1.18.3.

## Sign-offs

| Role | Name | Decision | Timestamp |
|---|---|---|---|
| Release owner | TokenPak | Complete | 2026-08-07T21:20:00Z |
| Independent review | TokenPak | Accepted on the exact head (acceptance-of-record on the release pull request at `d6c5ace825`; all seven required contexts verified by name against live branch protection; 49/49 check runs green; one blocking finding at the prior head — an internal reference in this log's classification note — fixed and re-reviewed after a formal head-movement stop). Independent closeout acceptance recorded at closure. | 2026-08-07 |
| Rollback decider | TokenPak | Rollback path retained and unexercised: 1.18.2 environments and runtime-pointer backups preserved on every deployment host. | 2026-08-07T21:20:00Z |

## Stage 0 — Change readiness

| Area | Status | Evidence |
|---|---|---|
| Scope and compatibility | Pass | Behavioral change is opt-in (lean profile) or guidance-only; dispatch unfiltered; HTTP contracts, wire formats, and storage schemas unchanged; one additive public symbol. |
| Focused implementation checks | Pass | Both feature waves carried independent adversarial review on their exact heads (staging #701 review-of-record, #702 review-of-record), each rerunning the full companion suite (776→779 passed, 17 skipped, 0 failures) plus targeted correctness, contract-stability, consumer-sweep, and public-safety checks. |
| Release-gate checks | Pass | `make release-gate-check` exit 0 at the cut head in the CI-parity environment (dev,full,serve,telemetry,tokens,otel extras; isolated HOME). Public-API snapshot regenerated: delta is exactly the version fields plus the additive `active_tools` symbol (4,634 symbols; an initial narrow-venv regen showing 667 spurious removals was detected as import-error noise and discarded). |
| Public identity and leak safety | Pass (pending PR contexts) | Diffs, commit messages, and PR bodies scanned clean; identity/language delta check to be confirmed green on the release PR. |
| Documentation | Pass | Changelog section complete with upgrade/rollback commands and compatibility disposition; doc example payloads advanced to 1.18.3 (9 literals across 5 files); this release record created at cut. |

## Stage 1 — Integration preparation

- **Release branch:** `release/v1.18.3` from staging main `290eef88d2`
- **Version surfaces:** `tokenpak/__init__.py` (pyproject reads via attr), `version.json` `tokenpak_version`, changelog section, API snapshot `package_version`, doc example payloads. `pinned_docs_revision` advances at docs propagation, not at cut.
- **Staging pull request:** pending
- **Build:** governed `release.yml` rebuilds at tag push; local rehearsal recorded in Stage 2.

## Stage 2 — Validation

| Check | Result | Evidence |
|---|---|---|
| Release-gate checks | Pass | `make release-gate-check` exit 0 at the cut head, CI-parity extras, isolated HOME. |
| Companion/feature suites | Pass | Code content identical to staging `290eef88d2`, where the full companion suite ran green twice (author lane and independent reviewer lane: 779 passed, 17 skipped, 0 failures); the cut adds only version, changelog, snapshot, changeset, docs-payload, and release-record surfaces. |
| Build + artifact integrity (local rehearsal) | Pass | Exactly one wheel + one sdist; `twine check` PASSED both; wheel contains zero paid/`packages/` content; sdist contains zero `_internal/` paths. Local hashes: wheel `19c5589e53…`, sdist `52ec2f5607…` (rehearsal only — governed `release.yml` rebuilds at tag push). |
| Clean install + smoke | Pass | Fresh venv, neutral cwd, isolated HOME: `tokenpak --version` → `tokenpak 1.18.3`; `tokenpak doctor` exit 0. |
| Upgrade from 1.18.2 | Pass | Fresh venv: PyPI `tokenpak==1.18.2` upgraded in place to the local 1.18.3 wheel; version transitioned; doctor exit 0. |
| Rollback to 1.18.2 | Pass | Same venv, reverse direction: `tokenpak==1.18.2` over the 1.18.3 install; version reverted; doctor exit 0. |
| PR required contexts | Pass | All seven required contexts green on exact head `d6c5ace825`, verified by name against live branch protection; 49/49 total check runs green including the advisory tier. |
| Independent exact-head acceptance | Pass | Acceptance-of-record posted on the release pull request: cut-surface purity (11 files, zero runtime code), snapshot delta exactly the additive symbol, changelog claims traced to code (including the 1.18.2 addendum ancestry proof), version-literal sweep, snapshot --check reproduction, artifact rebuild with content-list comparison, identity and public-safety scans. |

## Stage 3 — Go / no-go

Answered in words before tagging; decision **GO**.

1. All technical gates passed on the exact release commit (49/49 check runs; required contexts verified by name; release-gate checks exit 0 in the CI-parity environment).
2. Staging validation had zero failed checks (Stage 2 table; content suites green twice at the content commit).
3. Release notes and this record were complete and independently reviewed against code truth.
4. The rollback runbook is known to work for this change class: pip rollback exercised in both directions in rehearsal; per-host runtime-pointer anchors recorded before deployment.
5. No breaking change shipped unannounced: the API-snapshot delta is exactly the disclosed additive symbol; the classification decision is recorded above.
6. An active watcher was committed through publication and the post-deploy window.

## Stage 4 — Publication and deployment

| Step | Result | Evidence |
|---|---|---|
| Public promotion | Pass | Governed fast-forward of public `main` `88defbf7df` → `a587d48fba` (pure fast-forward, no new node; tree byte-identical to the accepted head). |
| Tag and workflow | Pass | Annotated `v1.18.3` (canonical identity) peels to `a587d48fba`; tag-push run 31212292445 green end to end (14 test shapes, snapshot validation, leak scan, chaos gate, build, Release, publish). |
| Publication | Pass | PyPI serves 1.18.3; wheel and sdist digests identical across the Release checksums, the index-declared values, and independently re-downloaded artifacts; provenance verification exit 0; clean production-index install reports 1.18.3 with doctor exit 0. |
| Ordering finding | Recorded | The GitHub Release was created (~19:55Z) before the PyPI publish completed (~20:17Z): only the publish job sits behind the deployment-environment gate, so approval latency opened a Release-before-publish window. No integrity impact; a workflow-topology correction is filed to ride its own change process. |
| Host deployment | Pass | All three deployment hosts: rollback anchor recorded first, per-version environment installed (including the tokenizer extra), runtime pointer flipped, proxy restarted; each reports version 1.18.3 healthy and the CLI reports 1.18.3 from a neutral directory. Version floor raised to 1.18.3 with the deployment health check passing. |
| Post-deploy watch | Operator-directed | Automated 4-hour watch armed at publication and terminated early (~45 minutes, zero anomalies) by explicit operator instruction; the operator directed that post-deploy validation proceed via their own manual testing. Any defect surfaced by that testing routes as a normal post-release defect; rollback anchors are retained. |


## Upgrade

```bash
python -m pip install --upgrade "tokenpak==1.18.3"
```

No operator-run data migration is required.

## Rollback

```bash
python -m pip install --upgrade "tokenpak==1.18.2"
```

Published artifacts are immutable. Deployment retains verified 1.18.2
environments and runtime-pointer backups until closeout.

## Compatibility disposition

- No breaking change. One additive public symbol (see classification note).
- Configurations not opting into the `lean` companion profile see no
  behavioral change; hooks cover cost estimation and budget enforcement
  out-of-band in all profiles.
- HTTP contracts, TIP wire formats, public storage schemas unchanged.
