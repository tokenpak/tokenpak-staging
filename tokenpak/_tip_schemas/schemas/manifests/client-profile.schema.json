{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tokenpak.ai/schemas/manifests/client-profile-v1.json",
  "title": "TIP-1.0 Client Profile",
  "description": "Per-client inbound profile (CLI, TUI, API, IDE, cron, batch). Describes how requests from this client kind should be routed and whether the companion should attach. See https://tokenpak.ai/protocol/manifest-spec.",
  "type": "object",
  "required": [
    "tip_version",
    "id",
    "name",
    "version",
    "kind",
    "capabilities",
    "compatibility",
    "client"
  ],
  "additionalProperties": false,
  "properties": {
    "tip_version": {
      "type": "string",
      "pattern": "^TIP-\\d+\\.\\d+$"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "name": {
      "type": "string"
    },
    "version": {
      "type": "string"
    },
    "kind": {
      "const": "client-profile"
    },
    "capabilities": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(tip|ext)\\.[a-z0-9._-]+$"
      }
    },
    "compatibility": {
      "$ref": "https://tokenpak.ai/schemas/tip/compatibility-v1.json"
    },
    "client": {
      "type": "object",
      "required": [
        "mode"
      ],
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "cli",
            "tui",
            "api",
            "sdk",
            "ide",
            "cron",
            "batch"
          ],
          "description": "How the client is consumed by the user."
        },
        "companion_eligible": {
          "type": "boolean",
          "description": "Whether the companion pre-send optimizer attaches for this client. True for cli/tui/ide; false for api/sdk (which get full proxy pipeline instead per Constitution \u00a73)."
        },
        "detection": {
          "type": "object",
          "description": "How TokenPak detects this client at runtime (per Constitution \u00a75.4 discovery).",
          "properties": {
            "header_signature": {
              "type": "string",
              "description": "Header pattern this client emits (e.g. 'X-Claude-Code-*'). Used by profile-detection in routing."
            },
            "env_signature": {
              "type": "string",
              "description": "Environment variable pattern the client sets."
            }
          }
        }
      }
    },
    "extensions": {
      "type": "object",
      "additionalProperties": true
    }
  }
}