# Waiver — <requirement being waived>

> Template. Replace everything in angle brackets. Delete this line.
> Referenced by: `GOVERNANCE.md` section 9.
> **Any missing field below means no waiver exists.** This is not a formality — an unexpiring,
> unowned waiver is how a standard quietly stops applying.

| Field | Value |
|---|---|
| **Waiver ID** | `<identifier>` |
| **Requirement waived** | `<standard ID + requirement number, e.g. BS-SWDEL-…-EVIDENCE R2>` |
| **Scope** | `<the single instance this applies to — release, artifact, environment, task>` |
| **Authorized by** | `<actor, and the authority they hold>` |
| **Granted** | `<YYYY-MM-DD>` |
| **Expires** | `<YYYY-MM-DD — mandatory, never "n/a">` |
| **Review date** | `<YYYY-MM-DD>` |

## Reason

<Why the requirement cannot be met here. "It was inconvenient" is a valid reason to record — and
recording it honestly is what makes the waiver count meaningful later.>

## Compensating control

<What is being done instead to manage the risk. If nothing, say "none" explicitly — an
uncompensated waiver is a different and larger decision.>

## Risk accepted

<What could go wrong because of this waiver, and who bears it.>

## Category check

- [ ] This waiver does **not** cross a protected category — it does not make a prohibited action
      allowed, a non-delegable action delegable, or a human-authorized action automatic
      (`GOVERNANCE.md` R12).

<If you cannot tick this, stop. No waiver can do that.>

## Repayment

| What was deferred | Owner | Due |
|---|---|---|
| `<deferred evidence or step>` | `<who>` | `<when>` |

## Prior waivers of this requirement

| Waiver ID | Date | Still relevant? |
|---|---|---|
| `<id>` | `<date>` | |

<Repeated waiving of the same requirement forces a decision: fix the requirement, fix the practice,
or record why it does not apply (`GOVERNANCE.md` R32). Count them here.>
